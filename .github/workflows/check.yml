name: Portfolio Check

on:
  push:
    branches:
      - main

jobs:
  check-repo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show Repository Structure
        run: ls -R

      - name: Success Message
        run: echo "Backend portfolio looks good 🚀"
